name: emoji
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.1.3

dependencies:
  cloud_firestore: ^5.6.6
  device_info_plus: ^11.4.0
  firebase_auth: ^5.5.2
  firebase_core: ^3.13.0
  flutter:
    sdk: flutter
  flutter_naver_map: ^1.3.1
  flutter_riverpod: ^2.6.1
  geocoding: ^3.0.0
  geolocator: ^14.0.0
  lottie: ^3.3.1
  provider: ^6.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/lottie/
    - assets/image/
    - assets/emoji/CommentImage